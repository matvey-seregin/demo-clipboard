
#Область ОбработчикиСобытийФормы

&НаКлиенте
Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)
	
	Если ИмяСобытия = "Приложение_ВставкаИзБуфераОбмена" Тогда
		Если ТипЗнч(Параметр) = Тип("Картинка") Тогда
			Информация = "Вставлена картинка";
		Иначе
			Информация = "Вставлены ссылки на файлы";
		КонецЕсли; 
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
